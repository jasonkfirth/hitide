# trout

`trout` is a compact tree-based router used by Hitide for matching HTTP paths.

The crate is vendored here so Hitide can keep its existing route behavior while
the server transport layer and dependencies are updated.
