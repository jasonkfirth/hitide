# Hitide Icons

This helper crate builds the generated icon lookup table used by Hitide.

It keeps icon metadata out of the main frontend crate while still allowing the
renderer to refer to icons by stable names.
